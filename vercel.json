{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "github": {
    "silent": true
  },
  "ignoreCommand": "if [[ \"$VERCEL_GIT_COMMIT_REF\" == claude/* ]] && [[ \"$VERCEL_GIT_COMMIT_REF\" != claude/ecommerce-store* ]]; then exit 0; else exit 1; fi"
}
